const flavors = [
    'vanilla',
    'chocolate',
    'Cherry Garcia',
    'Neopolitan',
    'French vanilla',
    'bubble gum',
    'butter pecan'
  ];

  const newArray = flavors.map(function(element) {
    return element;
  });

alert(element);
